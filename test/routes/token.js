describe("Routes: Token", () => {
  describe("POST /token", () => {
    it("returns authenticated user token", (done) => {
      // Código do teste...
    });

    it("throws status 401 when password is incorrect", (done) => {
      // Código do teste...
    });

    it("throws status 401 when email not exist", (done) => {
      // Código do teste...
    });

    it("throws status 401 when email and password are blank", (done) => {
      // Código do teste...
    });
  });
});
